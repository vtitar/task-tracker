# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    App\Domain\Task\Service\TaskBuilder\TaskBuilder:
        public: true

    App\Domain\Task\Service\TaskBuilder\TaskBuilderInterface: '@App\Domain\Task\Service\TaskBuilder\TaskBuilder'

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    App\DataFixtures\:
        resource: '../src/DataFixtures'
        arguments:
            $passwordHasher: '@security.user_password_hasher'
            $taskBuilder: '@App\Domain\Task\Service\TaskBuilder\TaskBuilderInterface'
            $taskRepository: '@App\Domain\Task\Repository\TaskRepository'
        tags: [ 'doctrine.fixture.orm' ]

    task.subscriber.search:
        class: \App\Domain\Task\EventSubscriber\TaskSearchSubscriber
        tags: [ { name: kernel.event_subscriber } ]

    task.subscriber.lifecycle:
        class: \App\Domain\Task\EventSubscriber\TaskLifecycleSubscriber
        tags: [ { name: kernel.event_subscriber } ]
